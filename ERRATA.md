# 勘误与更新

这本书写的是一个 `0.1.0-rc` 阶段的项目，官方 README 明写会有破坏性变更。这份文件记录两类内容：

1. **勘误** —— 书里写错的地方
2. **时效更新** —— dsh 变了，书里的描述已经不准

## 怎么提

在 [GitHub Issues](https://github.com/diguike/book-deepseek-harness/issues) 开 Issue，标题带上章节号。如果是时效问题，附上你机器上的版本：

```sh
npm view @deepseek-ai/dsh version
dsh --profile web --dump-config | head -20
```

## 版本水位线

| | 值 |
|---|---|
| 源码基准 | `47f94385`（2026-08-13，声明 `0.1.0-rc.5`） |
| 运行基准 | npm `0.1.0-rc.6` |
| 数据采集 | 2026-08-15 至 08-17 |
| 环境 | Ubuntu 20.04.6 LTS / Node v24.14.0 |

每章 frontmatter 的 `verified_against` 标明该章核对于哪个版本、哪一天。

## 自查清单：哪些内容最可能先过期

写作时就标出来的高风险区，按风险从高到低：

| 内容 | 章 | 风险来源 |
|---|---|---|
| `subagent-claude-code` / `subagent-codex` 的机制细节 | 16 | **双重依赖**：dsh 变会变，Claude Agent SDK / Codex CLI 变也会变 |
| `tool-cordis` 自修改 | 17 | 实验性功能，不在任何出厂配置树里 |
| typert / api-gateway / conversation node | 17 | dsh 里最年轻的子系统 |
| **patch 整体替换而非深合并** | 3.5、12 | 纯取舍、用户痛感明显、社区吐槽集中。**一旦改成深合并，第 12 章核心论证和 3.5 节同时作废** |
| 各包在 npm 上的版本 | 4.4、16.2 | 边缘包版本会掉队，peer 约束随时可能变 |
| compaction 的默认参数 | 15.3 | 调参是常见改动 |
| 前缀缓存的具体数值 | 11.2 | provider 侧的缓存实现会变；**机制（逐字节匹配前缀）稳定，数值不稳定** |

第一到三部分和第五部分写的是不变量层——事件模型、接缝模型、日志即真相、配置合成规则、工程方法。这些不跟着 rc 版本号变。

## 已知勘误

### 尚无

首版尚未收到勘误。发现问题请开 Issue。

## 写作过程中自己发现并修正的错误

这一节保留下来，因为它说明了本书的取证纪律是怎么来的，也提醒读者：**引用二手描述容易出系统性偏差**。

第一轮调研时我统计过五个数字，**五个全错，而且错的方向完全一致——都往「更宏大、更自动化、更严格」偏**：

| 最初写的 | 实际 | 错因 |
|---|---|---|
| Agent Notes 1,372 篇 | **688 篇** | 中英双语，`.md` 和 `.zh.md` 数了双倍 |
| docs 324 篇双语 | **英文 110 + 中文 105** | 324 是含 `.i18n.yaml` 的文件总数 |
| vendor 本地修改 8 条 | **18 条**（3 条动内核） | 记忆出错，没回查 |
| AGENTS.md 约 40 条规则、几乎每条配门禁 | **33 条**，真门禁约 **26 个** | 拍脑袋；34 个 `verify-*.ts` 文件里 8 个是验证器自己的测试 |
| 生成器「会校验」`@mode` 一致性 | **不会**，只保证每个声明事件至少有一个 dispatcher | 官方原文是 "can check"，我把情态动词升级成了断言 |

根因是「读文档 + 数文件」这条取证路径的系统性偏差：dsh 的文档质量高到会让人放松警惕，于是它的自我描述被当成一手事实转述了。

由此定下三条纪律（前言里对读者公开）：

1. 每个数字必须附上产生它的那条命令，汇总在附录 A
2. 每一条「dsh 会 / 必须 / 强制 X」的断言，取证必须落到代码或门禁脚本；落不到的降级为「文档如此描述」
3. **凡是让 dsh 显得更强的论断，取证标准比让它显得更弱的论断高一档**

第 19 章 19.1 节把这份对照表原样写进了正文。

## 实验方法的一次修正

第 11 章的前缀缓存实验，第一版方法是错的：**给每个变体连发两次、读第二次的读数**。那测的是变体自己的缓存，不是基线的——结果所有变体都显示 98.9%，看着毫无差别。

正确方法是「每个变体独立 salt，先捂热该支基线，再测改动后的**第一次**请求」。修正后的数据才出现 0% 和 98.8% 的分野。

这段过程写进了第 11 章 11.2 节，因为它是一个容易犯的实验设计错误。

## 没能实测的部分

按取证纪律，书里明确标注了三处没跑通的：

| 内容 | 章 | 原因 |
|---|---|---|
| `subagent-claude-code` 的运行行为 | 16.8 | npm 版本掉队在 `0.0.1-rc.1`，peer 冲突装不上；强装会破坏整个装置 |
| `tool-cordis` 自修改 | 17.5 | 同上，且不在任何出厂配置树里 |
| E2B 远程沙箱 | 13 | 本机到公网不通，用 Docker 同机容器替代，并明确标注「同机容器 ≠ 远程」 |

这三处讲的机制来自源码和 README，**不是运行观察**。如果你要在生产里用，各章末尾都列了自己该跑的验证项。

## 数据复现

全书所有数字的采集命令在[附录 A](appendix/a-mini-dsh.md)。原始产物在 `assets/`：

```
assets/ch01/   dump-config 全文、会话日志、请求录制、环境与采集命令
assets/ch11/   前缀实验脚本与十变量结果
assets/ch13/   本地 vs 容器延迟，12 次采样
assets/ch15/   六轮调用的真实用量轨迹
```

第 11 章的实验可以直接复现：

```sh
DEEPSEEK_API_KEY=... python3 assets/ch11/prefix-experiment.py
```

**你机器上的数字会不一样**，尤其是启动时间、内存和延迟。重要的是量级和比例。
